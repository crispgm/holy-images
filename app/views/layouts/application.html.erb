<!DOCTYPE html>
<html>
  <head>
    <title><%= t(:title) %></title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" type="image/x-icon" href="/holy-image-48.png">
    <link rel="stylesheet" href="/instagram.min.css">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <header class="menu">
      <div class="menu-wrapper">
        <div class="site-menu">
          <%= render "layouts/user" %>
        </div>
        <div class="site-name">
          <%= link_to t(:title_short), controller: "index", action: "index" %>
        </div>
      </div>
    </header>
    <div class="container">
      <%= yield %>

      <footer>
        <%= link_to t(:title_short), controller: "index", action: "index" %> &copy; 2018</a>
      </footer>
    </div>
  </body>

</html>
